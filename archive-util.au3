#include <File.au3>
#include <FileConstants.au3>
#include <WinAPIFiles.au3>
#pragma compile(Icon, 'archive.ico')

MsgBox(1, "Config Error", "Youdou dict doesn't exists in path Please setup your config.ini")